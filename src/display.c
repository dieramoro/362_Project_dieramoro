#include "display.h"

void clear_screen() {
    // Clear the display buffer (replace with library call)
}

void draw_note(int row, int column) {
    // Draw a note at the specified position (replace with library call)
}

void draw_strum_zone(int strum_zone) {
    // Highlight the strum zone on the display (replace with library call)
}

void draw_score(int score) {
    // Display the score (replace with library call)
}

void refresh_screen() {
    // Refresh the display (replace with library call)
}

void display_game_over(int score) {
    clear_screen();
    // Draw "Game Over" and score on the display
    refresh_screen();
}
